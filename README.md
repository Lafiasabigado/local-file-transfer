 How to Run:
   1. Navigate to the project folder: cd local-file-transfer
   2. Start the server: node server.js
   3. Access the app:
      - On the host machine: http://localhost:3000
      - On other devices: http://[YOUR_LOCAL_IP]:3000 (The IP will be printed in the terminal when the server starts).
